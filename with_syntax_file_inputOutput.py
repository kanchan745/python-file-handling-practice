# with open("created_new.textfile",'r')as f:#as = alias : giving same things but different name
#    data = f.read()
#    print(data)

# with open("created_new.textfile",'w')as f:
#     f.write("kanchan")


#deleting a file
#using the os module
#module (like a code library)is a file written by another programmer that generally has a function we can use...

'''import os
#pip pakage installer for python : if we want want install the module that is not pre installed syntax pip install lib_name

os.remove('File_i/demo.txt')'''
    